# Configuration belongs in the root .env. Run pnpm local:init.
# This file is documentation only; local.mjs never loads it.

# Keep the container stop grace longer than the Web drain deadline.
WEB_SHUTDOWN_TIMEOUT_MS=30000
WEB_STOP_GRACE_PERIOD=40s
