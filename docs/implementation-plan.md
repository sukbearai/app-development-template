# 应用模板实施记录

状态为改造完成。pnpm verify、独立备份恢复测试和最终源码的容器运行验证均通过。设计依据是 `analysis/template-effect-assessment.md`，用户已明确要求实施。历史分析仍描述改造前状态。

## 执行顺序

1. 读取 poteto-mode 原则并保存改造前快照。已完成，快照在 `.verification/template-implementation/before.tgz`。
2. Frame。以 9 页面、15 业务 HTTP 操作、13 表、迁移生成、后台及运维入口为能力基线。已完成。
3. Design。沿用已完成的四候选设计与评审，跳过重复 arena。采用 contracts → database → server 的依赖方向，Web 与 worker 为入口。
4. Run。工程基础、数据与身份、页面与上传、后台及中间件、运维分别形成可验证单元。
5. Audit。保留各独立目录的变更报告和实际命令结果。
6. Verify。运行类型、契约、业务测试、真实数据库、浏览器、构建及中间件检查；不能由静态通过推导运行通过。

## 并行边界

四个 GPT-6 Astra high 实施单元分别负责数据与服务器业务、worker、运维、契约。每个单元写独立副本，父线程审查并合并其指定文件。父线程负责工程骨架、Web、跨包连接与最终验收。

数据库模型保留用户、角色、权限、会话、文件、审计、遥测、outbox、任务及幂等实体。新增持久上传意图和 worker 租约用于恢复。一次业务写入的事务只由 database 包拥有，服务器用例包不创建第二套连接池。

## 工作量与完成条件

本次涉及旧模板三个 workspace 的迁移和两个共享包的拆分，以及五类基础设施与开发命令。主要未知是 vinext 对管理页面的兼容及后台故障恢复。优先验证工作区安装、共享事务、真实登录，随后扩大到其余入口。仅操作本次新建的隔离测试基础设施。

完整交付要求已实现能力有相应运行证据，已知缺陷有负向验证，启动与迁移文档和命令一致。Effect worker 试点维持后续可选阶段，不为首阶段引入 RC 运行时依赖。

## 已合入的实现

六个 workspace 已建立，原 9 页面、15 业务操作和 hello 路由均已迁入。契约生成、事务身份、上传协调、后台发布消费、运维和项目技能已连接到根命令。

独立审查发现并修复了权限时序、响应多余字段、数据库结构漂移、恢复冲突删除、Compose 参数遗漏和校验脚本中断问题。两份参考仓库始终只读。Git 发布状态以仓库提交历史为准。

实际命令证据在 `.verification/template-implementation/`，决策轨迹为 `decisions.tsv`。浏览器证据在 `.verification/pstack-x/`，应用制品和恢复证据在 `.verification/app/`，Docker 制品证据在 `.verification/containers/`。最终结果以各次运行输出为准。
